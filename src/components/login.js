import React from 'react';


const Login = ({options}) => {
    return(
        <div className="login">
        <a href="#">Log in</a>
          <i class="material-icons">
          person
          </i>
        </div>
    );
}

export default Login;
