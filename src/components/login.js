import React from 'react';


const Login = ({options}) => {
    return(
        <div className="login">
        <a href="#">Log in</a>
          <i className="material-icons">
          person
          </i>
        </div>
    );
}

export default Login;
